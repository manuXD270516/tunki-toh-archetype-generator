<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.3</version>
        <relativePath/>
    </parent>

    <groupId>pe.oh.sip</groupId>
    <artifactId>sip-toh-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>SIP TOH Parent</name>
    <description>Parent POM — Dependency management for all Tunki TOH v3.4 microservices</description>

    <properties>
        <!-- Java -->
        <java.version>21</java.version>

        <!-- Spring Boot aligned versions -->
        <spring-boot.version>3.4.3</spring-boot.version>

        <!-- Resilience4j -->
        <resilience4j.version>2.3.0</resilience4j.version>

        <!-- SIP shared libs -->
        <sip-common.version>1.0.0</sip-common.version>

        <!-- Test -->
        <testcontainers.version>1.20.4</testcontainers.version>

        <!-- Build -->
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ==================== SIP Shared Libraries ==================== -->
            <dependency>
                <groupId>pe.oh.sip</groupId>
                <artifactId>sip-core-client</artifactId>
                <version>${sip-common.version}</version>
            </dependency>
            <dependency>
                <groupId>pe.oh.sip</groupId>
                <artifactId>sip-exception-handler</artifactId>
                <version>${sip-common.version}</version>
            </dependency>
            <dependency>
                <groupId>pe.oh.sip</groupId>
                <artifactId>sip-endpoint-timeout</artifactId>
                <version>${sip-common.version}</version>
            </dependency>

            <!-- ==================== Resilience4j ==================== -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-bom</artifactId>
                <version>${resilience4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ==================== Testcontainers ==================== -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- Dependencies inherited by ALL child MS -->
    <dependencies>
        <!-- Spring Boot Web (MVC + Virtual Threads) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- MongoDB -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb</artifactId>
        </dependency>

        <!-- Redis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Actuator + Prometheus -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- AOP (required by Resilience4j annotations + @EndpointTimeout) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- Resilience4j -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
        </dependency>

        <!-- SIP Shared Libs (mandatory for all MS) -->
        <dependency>
            <groupId>pe.oh.sip</groupId>
            <artifactId>sip-core-client</artifactId>
        </dependency>
        <dependency>
            <groupId>pe.oh.sip</groupId>
            <artifactId>sip-exception-handler</artifactId>
        </dependency>
        <dependency>
            <groupId>pe.oh.sip</groupId>
            <artifactId>sip-endpoint-timeout</artifactId>
        </dependency>

        <!-- ==================== Test ==================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mongodb</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Azure Artifacts — inherited by all child MS -->
    <distributionManagement>
        <repository>
            <id>BackendArtifactsInDigitalpePro@Local</id>
            <name>InDigitalpe Backend Maven Feed</name>
            <url>https://pkgs.dev.azure.com/InDigitalpe/_packaging/BackendArtifactsInDigitalpePro%40Local/maven/v1</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>BackendArtifactsInDigitalpePro@Local</id>
            <name>InDigitalpe Backend Maven Feed</name>
            <url>https://pkgs.dev.azure.com/InDigitalpe/_packaging/BackendArtifactsInDigitalpePro%40Local/maven/v1</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
