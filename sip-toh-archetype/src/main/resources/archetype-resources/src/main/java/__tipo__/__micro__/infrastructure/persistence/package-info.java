/**
 * PERSISTENCE — MongoDB repository implementations.
 *
 * Example:
 * <pre>
 * public interface ProductCacheRepository extends MongoRepository<ProductCache, String> {
 *     Optional<ProductCache> findByUserId(String userId);
 * }
 * </pre>
 */
package ${package}.${tipo}.${micro}.infrastructure.persistence;
