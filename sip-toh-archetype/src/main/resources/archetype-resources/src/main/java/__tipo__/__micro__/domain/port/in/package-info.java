/**
 * INPUT PORTS — Use case interfaces that the API layer calls.
 *
 * Example:
 * <pre>
 * public interface CatalogUseCase {
 *     CatalogResponse getCatalog(String userId);
 * }
 * </pre>
 *
 * Dependency rule: api/ -> domain/port/in/ (allowed)
 */
package ${package}.${tipo}.${micro}.domain.port.in;
