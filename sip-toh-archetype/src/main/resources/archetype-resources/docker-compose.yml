#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
${symbol_pound} =============================================================================
${symbol_pound} Tunki TOH v3.4 - Local Development Infrastructure (${msName})
${symbol_pound} =============================================================================
${symbol_pound} Usage:
${symbol_pound}   docker compose up -d          # Start in background
${symbol_pound}   docker compose down           # Stop
${symbol_pound}   docker compose down -v        # Stop + delete volumes
${symbol_pound} =============================================================================

services:
  mongo:
    image: mongo:7.0
    container_name: ${msName}-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: ${dbName}
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7.4-alpine
    container_name: ${msName}-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongo_data:
  redis_data:
